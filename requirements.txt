numpy<2                      # <2 required: pupil-apriltags is built against the numpy 1.x ABI
opencv-contrib-python>=4.8   # contrib build: aruco, CLAHE and cornerSubPix are used
pupil-apriltags>=1.0.4       # AprilTag (tag36h11) detector
PyYAML>=6                    # config.yaml parsing
